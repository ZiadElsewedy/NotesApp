import 'package:flutter/material.dart';

class NoteWidegt extends StatelessWidget {
  const NoteWidegt({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      
    );
  }
}